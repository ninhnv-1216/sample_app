<li>
  <%= gravatar_for user, size: Settings.avt_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("delete_user"), user, method: :delete,
                                           data: { confirm: t(".sure") } %>
  <% end %>
</li>
